# Node.js / npm
# Ignore dependencies, as they can be re-installed with `npm install`
node_modules/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
# Never commit sensitive data like API keys or secrets.
.env
.env.local
.env.*.local

# Build output directory
dist
build

# Bruno API Client
# Ignore local workspace settings and environment variable overrides.
# Your API collections (.bru files) and bruno.json should be committed.
.bruno/
*.local.bru

# macOS specific
.DS_Store